<%= render "shared/navbars/main" %>

<div class="chat-room">

  <div class="chat-room__header">
    <picture class="user-avatar user-avatar--small chat-room__avatar">
      <img src="http://via.placeholder.com/50x50">
    </picture>

    <div class="chat-room__name">
      <%= @other_user.name %>
    </div>
  </div>

  <div class="chat-room__box">
    <% @chat_room.messages.each do |message| %>
      <% user_is_me = message.user == current_user %>
      <div class="chat-room__message <%= user_is_me ? "chat-inbox__message--me" : "chat-inbox__message--not-me" %>">
        <%= message.message %>
      </div>
    <% end %>
  </div>

  <%= simple_form_for @message, url: send_message_chat_path(@chat_room, format: :json), remote: true, html: { class: "chat-room__form" } do |f| %>
    <%= f.input :message, input_html: { class: "chat-room__message-input" } %>
    <%= f.button :submit, value: ">", input_html: { class: "chat-room__message-send" } %>
  <% end %>

</div>
